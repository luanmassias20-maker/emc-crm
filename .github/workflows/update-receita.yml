name: Arquivo - carga Dados Abertos Receita

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  arquivado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fluxo arquivado. O EMC CRM agora utiliza Consulta CNPJ SERPRO sob demanda."
